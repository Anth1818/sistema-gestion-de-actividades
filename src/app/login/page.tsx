import { LoginContainer } from "@/components/login";

export default function LoginPage() {
    return (
        <div className="w-full h-full flex justify-center mt-20 overflow-hidden">
            <LoginContainer/>
        </div>
    )
}